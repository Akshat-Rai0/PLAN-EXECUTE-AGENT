[project]
name = "browser-automation"
version = "0.1.0"
description = "Browser Use configured with OpenRouter and NVIDIA Nemotron 3 Nano Omni"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "browser-use>=0.13.6",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
